PASS: project selection through the native select change event; project-specific plugin rows, selection, breadcrumb, content, enabled-plugin view and settings; no cross-project Goal search or links; return restores plugin/Goal/camera/conversation and terminal drafts; existing plugin clicks and Goal search/list selection; desktop, 692px and mobile directory retains selection context then dismisses on plugin click; light/dark screenshots; no page errors. Native popup keystrokes could not be driven by headless macOS Chrome CDP (Home/End and Arrow keys left selection unchanged); project state checks use select change events.
