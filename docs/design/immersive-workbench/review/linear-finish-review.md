# 两层目录与插件市场独立审阅

2026-09-12 — disposition: ship

范围为独立设计原型，完成等级 2；审阅者使用新上下文，只读源码和截图，没有另开浏览器。

固定项目头部、同栏进入插件目录与返回、卡片市场和指定项目安装符合最新合同。紧凑系统字体、石墨中性色、细分隔与克制紫色符合 Linear 参考方向；浅色工作框也成立。演示与模拟安装标注清楚。

14 张要求截图均有效，覆盖桌面 1560×1027、手机 390×844、用户当前预览 529×936；未见市场、安装面板和目录抽屉截断主要操作。行为结论使用 `linear-navigation-qa.txt` 的主执行方运行结果，并结合源码检查。

没有 Blocker / P1。非阻塞 P2 留待后续：`workbench.js` 的 `openGoal` 仅在工作框首次打开的分支调用 `syncDirectorySelection`；已打开时换另一 Goal，第一栏可能保留原目标高亮。本轮不继续修改或追加截图。

交付边界：刷新重置演示状态，真实插件安装、持久化与 Runtime 接入均未实现。这次 ship 是原型设计审阅结论，不代表生产功能验收或发布。
